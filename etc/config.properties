# Legacy placeholder.
#
# The Docker Compose setup mounts role-specific Trino config files from:
# - etc/coordinator/config.properties
# - etc/worker/config.properties
#
# Keep this file only as a signpost for people who expect the classic
# /etc/trino/config.properties layout at the repository root.
