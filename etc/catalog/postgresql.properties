connector.name=postgresql
connection-url=jdbc:postgresql://${ENV:POSTGRESQL_HOST}:${ENV:POSTGRESQL_PORT}/${ENV:POSTGRESQL_DATABASE}
connection-user=${ENV:POSTGRESQL_USER}
connection-password=${ENV:POSTGRESQL_PASSWORD}
