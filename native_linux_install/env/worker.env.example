# Her worker sunucusunda bu dosyayi kopyalayip gercek degerleri doldur.
# Ornek: cp worker.env.example /tmp/trino-worker.env

TRINO_ENVIRONMENT=prod

# Her worker icin benzersiz olmali.
# Worker-1: trino-worker-01
# Worker-2: trino-worker-02
# Worker-3: trino-worker-03
TRINO_NODE_ID=trino-worker-01

# Buraya coordinator'in DNS/IP adresini yaz.
TRINO_COORDINATOR_URI=http://<coordinator-host>:8080

POSTGRESQL_HOST=<postgres-host>
POSTGRESQL_PORT=5432
POSTGRESQL_DATABASE=<database-name>
POSTGRESQL_USER=<trino-readonly-user>
POSTGRESQL_PASSWORD=<password>

ORACLE_HOST=<oracle-host>
ORACLE_PORT=1521
ORACLE_SERVICE=<oracle-service-name>
ORACLE_USER=<trino-readonly-user>
ORACLE_PASSWORD=<password>
