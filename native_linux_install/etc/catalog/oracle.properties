connector.name=oracle

# Oracle SERVICE_NAME formatidir.
# SID gerekiyorsa DBA'den dogru JDBC formatini alip bu satiri degistir.
connection-url=jdbc:oracle:thin:@//{{ORACLE_HOST}}:{{ORACLE_PORT}}/{{ORACLE_SERVICE}}
connection-user={{ORACLE_USER}}
connection-password={{ORACLE_PASSWORD}}
